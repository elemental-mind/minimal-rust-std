cargo-features = ["panic-immediate-abort"]

[package]
name = "minimal-rust-std-setup"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.release]
strip = true
opt-level= "z"
codegen-units = 1
lto = true
panic = "immediate-abort"